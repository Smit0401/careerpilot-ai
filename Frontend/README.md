# CareerPilot AI Frontend

React and Vite client for CareerPilot AI.

## Commands

- `npm run dev` starts the development server.
- `npm run build` creates a production build.
- `npm run lint` runs ESLint.

The API defaults to `http://localhost:3000`. Set `VITE_API_URL` in the frontend environment when deploying to a different origin.
